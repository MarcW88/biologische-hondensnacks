import fs from'node:fs';import path from'node:path';const root=path.resolve(import.meta.dirname,'..');let count=0,errors=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(['.git','.agents','node_modules'].includes(e.name))continue;const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(e.name.endsWith('.html')){count++;const s=fs.readFileSync(p,'utf8');if(!s.includes('name="robots" content="noindex,follow"'))errors.push(`${p}: robots ontbreekt`);if(!s.includes('assets/styles.css'))errors.push(`${p}: CSS ontbreekt`)}}}walk(root);if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log(`${count} HTML-pagina's gecontroleerd.`);
